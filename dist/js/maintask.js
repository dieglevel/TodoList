function skip(){
    var skip =[]
    for (var i = 0 ; i < mainTask.length ; i++){
        for (var j = 0 ; j < skip.length; j++){
            if (mainTask[i] != mainTask[j]){
                
            }
        }
    }
}